package com.poseidon.codegraph.model.event;

/**
 * 节点变更监听器 SPI
 * 下游模块（如向量化管道）实现此接口以感知节点级增删改事件
 */
@FunctionalInterface
public interface NodeChangeListener {

    void onNodeChanged(NodeChangeEvent event);
}
